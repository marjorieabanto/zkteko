package com.example.demo.service;import com.example.demo.model.Fingerprint;import com.example.demo.repository.FingerPrintRepository;import org.springframework.beans.factory.annotation.Autowired;import java.io.IOException;import java.util.List;public interface FingerPrintService {    boolean initializeSensor();    boolean startCapture();    byte[] captureFingerprint(String nombre);    void stopCapture();    boolean closeSensor();    void keepFingerprintWithUser(Fingerprint fingerprint) ;}